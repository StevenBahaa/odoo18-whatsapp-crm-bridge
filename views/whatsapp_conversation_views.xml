<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_whatsapp_conversation_list" model="ir.ui.view">
        <field name="name">whatsapp.conversation.list</field>
        <field name="model">whatsapp.conversation</field>
        <field name="arch" type="xml">
            <list string="WhatsApp Conversations" decoration-danger="needs_reply">
                <field name="last_message_at"/>
                <field name="name"/>
                <field name="account_id"/>
                <field name="partner_id"/>
                <field name="lead_id"/>
                <field name="assigned_user_id"/>
                <field name="phone"/>
                <field name="state"/>
                <field name="closed_at" optional="hide"/>
                <field name="closed_by_id" optional="hide"/>
                <field name="needs_reply"/>
                <field name="message_count"/>
                <field name="inbound_message_count"/>
                <field name="outbound_message_count"/>
            </list>
        </field>
    </record>

    <record id="view_whatsapp_conversation_form" model="ir.ui.view">
        <field name="name">whatsapp.conversation.form</field>
        <field name="model">whatsapp.conversation</field>
        <field name="arch" type="xml">
            <form string="WhatsApp Conversation">
                <header>
                    <button name="action_open_crm_lead" type="object" string="Open CRM Lead" class="btn-primary" invisible="not lead_id"/>
                    <button name="action_send_whatsapp_message" type="object" string="Send WhatsApp" class="btn-secondary" invisible="not lead_id"/>
                    <button name="action_mark_open" type="object" string="Mark Open" invisible="state != 'pending'"/>
                    <button name="action_mark_pending" type="object" string="Mark Pending" invisible="state != 'open'"/>
                    <button name="action_mark_closed" type="object" string="Close Conversation" class="btn-secondary" invisible="state == 'closed'"/>
                    <button name="action_mark_open" type="object" string="Reopen Conversation" class="btn-primary" invisible="state != 'closed'"/>
                    <field name="state" widget="statusbar" statusbar_visible="open,pending,closed"/>
                </header>

                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name"/>
                        </h1>
                    </div>

                    <div class="alert alert-warning" role="alert" invisible="not needs_reply">
                        <strong>Needs Reply</strong> — the customer has sent a newer inbound WhatsApp message than the latest outbound reply.
                    </div>

                    <group>
                        <group string="Customer">
                            <field name="account_id"/>
                            <field name="company_id"/>
                            <field name="partner_id"/>
                            <field name="lead_id"/>
                            <field name="assigned_user_id"/>
                        </group>

                        <group string="Phone">
                            <field name="phone"/>
                            <field name="normalized_phone"/>
                            <field name="needs_reply"/>
                        </group>
                    </group>

                    <group string="Lifecycle">
                        <group>
                            <field name="close_reason" placeholder="Optional reason before closing the conversation..." readonly="state == 'closed'"/>
                            <field name="lifecycle_note" readonly="1"/>
                        </group>
                        <group>
                            <field name="closed_at" readonly="1"/>
                            <field name="closed_by_id" readonly="1"/>
                            <field name="last_state_change_at" readonly="1"/>
                            <field name="last_state_change_by_id" readonly="1"/>
                        </group>
                    </group>

                    <group>
                        <group string="Message Timeline">
                            <field name="last_message_at"/>
                            <field name="last_inbound_at"/>
                            <field name="last_outbound_at"/>
                        </group>

                        <group string="Counters">
                            <field name="message_count"/>
                            <field name="inbound_message_count"/>
                            <field name="outbound_message_count"/>
                        </group>
                    </group>

                    <notebook>
                        <page string="Messages">
                            <field name="message_ids" readonly="1">
                                <list decoration-danger="status == 'failed'" decoration-success="direction == 'inbound'">
                                    <field name="create_date"/>
                                    <field name="direction"/>
                                    <field name="status"/>
                                    <field name="message_type"/>
                                    <field name="body"/>
                                    <field name="recipient_phone"/>
                                    <field name="external_message_id"/>
                                    <field name="assigned_user_id"/>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_whatsapp_conversation_search" model="ir.ui.view">
        <field name="name">whatsapp.conversation.search</field>
        <field name="model">whatsapp.conversation</field>
        <field name="arch" type="xml">
            <search string="Search WhatsApp Conversations">
                <field name="name"/>
                <field name="account_id"/>
                <field name="partner_id"/>
                <field name="lead_id"/>
                <field name="assigned_user_id"/>
                <field name="phone"/>
                <field name="normalized_phone"/>
                <field name="state"/>
                <field name="closed_by_id"/>

                <filter name="open" string="Open" domain="[('state', '=', 'open')]"/>
                <filter name="pending" string="Pending" domain="[('state', '=', 'pending')]"/>
                <filter name="closed" string="Closed" domain="[('state', '=', 'closed')]"/>
                <filter name="needs_reply" string="Needs Reply" domain="[('needs_reply', '=', True)]"/>
                <filter name="my_conversations" string="My Conversations" domain="[('assigned_user_id', '=', uid)]"/>
                <filter name="unassigned" string="Unassigned" domain="[('assigned_user_id', '=', False)]"/>

                <group expand="0" string="Group By">
                    <filter name="group_by_account" string="Account" context="{'group_by': 'account_id'}"/>
                    <filter name="group_by_company" string="Company" context="{'group_by': 'company_id'}"/>
                    <filter name="group_by_assigned_user" string="Assigned Salesperson" context="{'group_by': 'assigned_user_id'}"/>
                    <filter name="group_by_state" string="Status" context="{'group_by': 'state'}"/>
                    <filter name="group_by_closed_by" string="Closed By" context="{'group_by': 'closed_by_id'}"/>
                    <filter name="group_by_needs_reply" string="Needs Reply" context="{'group_by': 'needs_reply'}"/>
                    <filter name="group_by_create_date" string="Creation Date" context="{'group_by': 'create_date'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="view_whatsapp_conversation_pivot" model="ir.ui.view">
        <field name="name">whatsapp.conversation.pivot</field>
        <field name="model">whatsapp.conversation</field>
        <field name="arch" type="xml">
            <pivot string="WhatsApp Conversation Analysis">
                <field name="state" type="row"/>
                <field name="assigned_user_id" type="row"/>
                <field name="needs_reply" type="col"/>
            </pivot>
        </field>
    </record>

    <record id="view_whatsapp_conversation_graph" model="ir.ui.view">
        <field name="name">whatsapp.conversation.graph</field>
        <field name="model">whatsapp.conversation</field>
        <field name="arch" type="xml">
            <graph string="WhatsApp Conversations" type="bar">
                <field name="state"/>
            </graph>
        </field>
    </record>

    <record id="action_whatsapp_conversation" model="ir.actions.act_window">
        <field name="name">WhatsApp Conversations</field>
        <field name="res_model">whatsapp.conversation</field>
        <field name="view_mode">list,form,pivot,graph</field>
        <field name="context">{'search_default_open': 1}</field>
    </record>

    <menuitem
        id="menu_whatsapp_conversation"
        name="Conversations"
        parent="menu_whatsapp_crm_configuration"
        action="action_whatsapp_conversation"
        sequence="15"/>

</odoo>